"use client";

// Imports removed as they're no longer needed

export function CategoryNav() {
  // Filter and relevance row removed as requested
  return (
    <div className="w-full border-t border-b border-gray-200">
      <div className="container-fog py-3">
        {/* Empty space where filter and relevance used to be */}
      </div>
    </div>
  );
}
